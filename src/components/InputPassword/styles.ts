import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  height: 5.6rem;
  width: 100%;
  background-color: #afaddd;
  input {
    height: 100%;
    width: 90%;
  }
  label {
    position: absolute;
  }
`;
